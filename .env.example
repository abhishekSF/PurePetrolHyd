# Optional. Leave unset to use local PGLite.
# DATABASE_URL=postgres://...

# Set on grok.me publish automatically. Set this to your domain when self-hosting.
# VITE_PUBLIC_HOSTNAME=purepetrolhyd.in

# Optional auth (Google / X). The map works without sign-in.
# VITE_AUTH_ENABLED=false
